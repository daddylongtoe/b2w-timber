{% extends 'base.twig' %}

{% block content %}
  {% include 'banner-title.twig' %}

  <div class="content-area">
    <div class="container">
      <div class="row">
        {% include 'featured-image.twig' %}
        <div class="col-md-8 offset-md-2 overflow-hidden">
          {% block template %}
            {# Page template content to go here #}
            Empty!
          {% endblock %}
        </div>
      </div>
      {% include 'pagination.twig' %}
    </div>
  </div>
{% endblock %}
